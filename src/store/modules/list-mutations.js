export default class ListMutations {
    setCurrentPage(state, page) {
        state.currentPage = page;
    }
    setPageSize(state, pagesize) {
        state.pageSize = pagesize;
    }
}
//# sourceMappingURL=list-mutations.js.map